# 📊 System Architecture - Channel & Video Flow

## 🔄 System Flow Diagrams

### 1️⃣ User Registration Flow
```
┌─────────────────┐
│   User Opens    │
│   Register Page │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│ Fills: email, password, │
│ username                │
└──────────┬──────────────┘
           │
           ▼
    POST /api/auth/register
    {
      email, password, username
    }
           │
           ▼
┌──────────────────────────┐
│  Backend Creates User    │
│  in users table          │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  Backend Auto-Creates    │ ✅ NEW
│  Channel in channels     │
│  table                   │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  Backend Updates         │
│  user.channel_id         │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  Response:               │
│  {                       │
│    token,                │
│    user: {               │
│      channel_id: "uuid"  │ ✅ Present
│    }                     │
│  }                       │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  User Can Upload Videos  │ ✅ Works
└──────────────────────────┘
```

---

### 2️⃣ Existing User Login Flow
```
┌─────────────────┐
│   User Opens    │
│   Login Page    │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│ Enters: email, password │
└──────────┬──────────────┘
           │
           ▼
    POST /api/auth/login
    { email, password }
           │
           ▼
┌──────────────────────────┐
│  Backend Validates User  │
└──────────┬───────────────┘
           │
           ▼
      ┌───────────┐
      │ Has       │
      │ channel_id?│
      └─────┬─────┘
            │
      ┌─────┴──────┐
      │            │
     YES          NO
      │            │
      │            ▼
      │    ┌──────────────────┐
      │    │ Backend Creates  │ ✅ NEW
      │    │ Channel          │
      │    └────────┬─────────┘
      │             │
      │             ▼
      │    ┌──────────────────┐
      │    │ Updates          │
      │    │ user.channel_id  │
      │    └────────┬─────────┘
      │             │
      └─────────────┘
                    │
                    ▼
           ┌──────────────────┐
           │  Response:       │
           │  {               │
           │    token,        │
           │    user: {       │
           │      channel_id  │ ✅ Always present
           │    }             │
           │  }               │
           └────────┬─────────┘
                    │
                    ▼
           ┌──────────────────┐
           │ User Can Upload  │ ✅ Works
           └──────────────────┘
```

---

### 3️⃣ Video Upload Flow
```
┌─────────────────┐
│   User Selects  │
│   Video + Info  │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend Creates        │
│ FormData:               │
│  - video file           │
│  - thumbnail            │
│  - title                │
│  - description          │
│  - category             │
│  - privacy              │
│  - tags                 │
└──────────┬──────────────┘
           │
           ▼
    POST /api/video/upload
    Headers: Authorization: Bearer {token}
    Body: FormData
           │
           ▼
┌──────────────────────────┐
│  Backend Validates Token │
│  Gets User Info          │
└──────────┬───────────────┘
           │
           ▼
      ┌───────────┐
      │ User has  │
      │ channel_id?│
      └─────┬─────┘
            │
      ┌─────┴──────┐
      │            │
     YES          NO
      │            │
      │            ▼
      │    ┌──────────────────┐
      │    │ Return Error:    │ ❌ Shouldn't happen
      │    │ "Channel not     │    (users have channels now)
      │    │  found"          │
      │    └──────────────────┘
      │
      ▼
┌──────────────────────────┐
│  Backend:                │
│  1. Uploads video file   │
│  2. Uploads thumbnail    │
│  3. Creates video record │
│     with channel_id      │ ✅ Valid FK
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  SQL INSERT:             │
│  INSERT INTO videos (    │
│    user_id,              │
│    channel_id,           │ ✅ Valid (no FK error)
│    title,                │
│    video_url,            │
│    ...                   │
│  )                       │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  Response:               │
│  {                       │
│    success: true,        │
│    video_id,             │
│    video_url,            │
│    thumbnail_url         │
│  }                       │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  Frontend Shows Success  │ ✅ Works
│  Video Appears in List   │
└──────────────────────────┘
```

---

## 🗄️ Database Relationships

```
┌─────────────────────┐
│      users          │
│─────────────────────│
│ id (PK)             │◄──────┐
│ username            │       │
│ email               │       │
│ channel_id (FK) ────┼───┐   │
│ ...                 │   │   │
└─────────────────────┘   │   │
                          │   │
                          │   │
                          ▼   │
                  ┌─────────────────────┐
                  │     channels        │
                  │─────────────────────│
                  │ id (PK)             │
                  │ user_id (FK) ───────┘
                  │ name                │
                  │ handle (UNIQUE)     │
                  │ ...                 │
                  └──────────┬──────────┘
                             │
                             │
                             │
                             │
                  ┌──────────▼──────────┐
                  │      videos         │
                  │─────────────────────│
                  │ id (PK)             │
                  │ user_id (FK)        │
                  │ channel_id (FK) ────┘ ✅ Required!
                  │ title               │
                  │ video_url           │
                  │ ...                 │
                  └─────────────────────┘
```

### Foreign Key Constraints
```
videos.user_id    → users.id        (CASCADE DELETE)
videos.channel_id → channels.id     (CASCADE DELETE) ⚠️ Must be valid
channels.user_id  → users.id        (CASCADE DELETE)
users.channel_id  → channels.id     (SET NULL)
```

---

## 📋 Data Flow Example

### Example 1: New User "john_doe"

```
Step 1: Registration
──────────────────────
POST /api/auth/register
{ email: "john@example.com", password: "pass123", username: "john_doe" }

Backend creates:
  users:
    id: "user-uuid-001"
    username: "john_doe"
    email: "john@example.com"
    channel_id: NULL  ← Initially empty

  channels:
    id: "channel-uuid-001"
    user_id: "user-uuid-001"
    name: "john_doe's Channel"
    handle: "@johndoe_abc123"

  users (updated):
    channel_id: "channel-uuid-001"  ← Now linked

Response:
{
  token: "session-token-xyz",
  user: {
    id: "user-uuid-001",
    username: "john_doe",
    channel_id: "channel-uuid-001"  ✅ Present
  }
}
```

### Example 2: Video Upload by "john_doe"

```
Step 2: Upload Video
────────────────────
POST /api/video/upload
Headers: Authorization: Bearer session-token-xyz
Body: FormData {
  video: video_file.mp4,
  thumbnail: thumb.jpg,
  title: "My First Video",
  category: "Gaming"
}

Backend:
  1. Extracts user from token → user_id: "user-uuid-001"
  2. Gets channel_id from user → channel_id: "channel-uuid-001"
  3. Validates channel exists ✅
  4. Uploads files
  5. Creates video record:

  videos:
    id: "video-uuid-001"
    user_id: "user-uuid-001"
    channel_id: "channel-uuid-001"  ✅ Valid FK constraint
    title: "My First Video"
    video_url: "https://moviedbr.com/uploads/videos/uuid.mp4"
    thumbnail: "https://moviedbr.com/uploads/thumbnails/uuid.jpg"

Response:
{
  success: true,
  video_id: "video-uuid-001",
  video_url: "...",
  thumbnail_url: "..."
}
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│ Login/Signup │
└──────┬───────┘
       │
       ▼
  Generate JWT Token
       │
       ▼
┌─────────────────┐
│  Token Contains │
│  - user_id      │
│  - role         │
│  - exp          │
└────────┬────────┘
         │
         ▼
    Store in:
    - SecureStore (mobile)
    - AsyncStorage (web)
         │
         ▼
┌─────────────────────┐
│ Every API Request   │
│ Authorization:      │
│ Bearer {token}      │
└────────┬────────────┘
         │
         ▼
    Backend validates:
    1. Token signature
    2. Token expiry
    3. User exists
         │
         ▼
┌─────────────────────┐
│ Extract user info   │
│ including           │
│ channel_id          │ ✅ Auto-populated
└────────┬────────────┘
         │
         ▼
    Use in video upload
```

---

## 🎯 Channel Creation Logic

```
┌─────────────────────────┐
│ Channel Creation Point  │
└────────┬────────────────┘
         │
    ┌────┴─────┐
    │          │
    ▼          ▼
Register    Login
    │          │
    └────┬─────┘
         │
         ▼
  ┌──────────────┐
  │ Check:       │
  │ channel_id   │
  │ exists?      │
  └──────┬───────┘
         │
    ┌────┴────┐
   YES       NO
    │         │
    │         ▼
    │  ┌─────────────────┐
    │  │ Generate UUID   │
    │  └────────┬────────┘
    │           │
    │           ▼
    │  ┌─────────────────┐
    │  │ Create name:    │
    │  │ "user's Channel"│
    │  └────────┬────────┘
    │           │
    │           ▼
    │  ┌─────────────────┐
    │  │ Create handle:  │
    │  │ @user_abc123    │
    │  └────────┬────────┘
    │           │
    │           ▼
    │  ┌─────────────────┐
    │  │ INSERT channels │
    │  └────────┬────────┘
    │           │
    │           ▼
    │  ┌─────────────────┐
    │  │ UPDATE users    │
    │  │ SET channel_id  │
    │  └────────┬────────┘
    │           │
    └───────────┘
                │
                ▼
        ┌───────────────┐
        │ User has      │
        │ channel       │ ✅
        └───────────────┘
```

---

## 📊 State Comparison

### ❌ Before Fix

```
User Registration
      ↓
  users table
    channel_id: NULL  ← Problem!
      ↓
Video Upload
      ↓
INSERT videos (channel_id: NULL)
      ↓
❌ Foreign Key Constraint Error
   "Cannot add or update a child row"
```

### ✅ After Fix

```
User Registration/Login
      ↓
  Auto-create channel
      ↓
  users table
    channel_id: "uuid"  ← Populated!
      ↓
Video Upload
      ↓
INSERT videos (channel_id: "uuid")
      ↓
✅ Success! Valid FK constraint
```

---

## 🔄 Error Handling Flow

```
Video Upload Request
        │
        ▼
   ┌─────────┐
   │ Token   │
   │ Valid?  │
   └────┬────┘
        │
   ┌────┴────┐
  NO        YES
   │          │
   ▼          ▼
 401       ┌─────────┐
Unauthorized│ User has│
           │channel_id│
           └────┬────┘
                │
           ┌────┴────┐
          NO        YES
           │          │
           ▼          ▼
         400      ┌───────────┐
      "Channel   │ File size │
       not found"│ OK?       │
                 └─────┬─────┘
                       │
                  ┌────┴────┐
                 NO        YES
                  │          │
                  ▼          ▼
                400      ┌───────┐
             "File too  │Upload │
              large"    │files  │
                        └───┬───┘
                            │
                            ▼
                      ┌──────────┐
                      │ Insert   │
                      │ to DB    │
                      └─────┬────┘
                            │
                            ▼
                          200 OK
                       ✅ Success
```

---

## 💡 Key Takeaways

1. **Users ↔ Channels**: 1-to-1 relationship (one user, one channel)
2. **Videos → Channels**: Many-to-1 relationship (many videos per channel)
3. **Auto-Creation**: Channels created automatically, no user action needed
4. **FK Constraints**: All relationships enforced at database level
5. **Backward Compatible**: Existing users get channels on next login
6. **Zero Frontend Changes**: All logic in backend
7. **Secure**: Channels tied to authenticated user, can't be hijacked

---

This architecture ensures data integrity while providing a seamless user experience!
